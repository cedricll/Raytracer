#include "Sphere.h"

// std::vector<double> Sphere::intersections(Ray r) {
// 	std::vector<double> inters;
// 	inters.push_back(abs(this->centerPoint.coord[2] - r.startpoint.coord[2]));
// 	inters.push_back(abs(this->centerPoint.coord[1] - r.startpoint.coord[1]));
// 	inters.push_back(abs(this->centerPoint.coord[0] - r.startpoint.coord[0]));

// 	return inters;
// };

// Sphere::Sphere(struct Point p, int r) {
// 	this->centerPoint = p;
// 	this->radius = r;
// };